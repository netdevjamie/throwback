# Strava API credentials — paste from https://www.strava.com/settings/api
# Then save this file as .env (drop the .template suffix)

STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=

# These two are filled in automatically by the OAuth step (do_oauth.py).
# Leave blank for now.
STRAVA_ACCESS_TOKEN=
STRAVA_REFRESH_TOKEN=
